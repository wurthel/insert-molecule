# The origin molecule
molecule_fn = Data/1M0L-exp.pdb

# The result
molecule_result_fn = 1M0L_result.pdb

# The inserted molecule in Z-matrix representation
zmolecule_fn = Data/zmatrix-1M0L-exp

# The number of first atoms that are inserted without optimization
n_without_opt = 0

# The range of atoms that are inserted with optimization
n_with_opt = (1, 29)
skip_opt = 4

# Optimization script path
opt_path = Optimizer/

# Optimization script
opt_script = bash relax.sh

# PDB file of an optimized molecule (for optimizer)
opt_mol_bef = 1M0L.pdb

# PDB file optimized molecule (for optimizer)
opt_mol_aft = 1M0L_0.pdb

# File where the amino acids that require optimization are displayed (for optimizer)
opt_resid = N
